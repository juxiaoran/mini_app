<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mini.app.mapper.AppointmentMapper">
    <insert id="save" parameterType="mini.app.entity.Appointment">
        INSERT INTO appointment
        (user_id, service_name, appoint_date, time_slot, created_at, updated_at)
        VALUES
        (#{userId}, #{serviceName}, #{appointDate}, #{timeSlot}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        updated_at = NOW()
    </insert>
</mapper>
